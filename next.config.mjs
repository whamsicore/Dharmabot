/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/src/app',
  // distDir: '/src/app/.next',
  // The basePath configuration option specifies the base path for the application.
  // This is the path where Next.js will start looking for your application's pages.
  // In this case, './src/app' sets the base path to the 'src/app' directory relative to the project root.
  basePath: '',
  // distDir: 'build',
};
export default nextConfig;
